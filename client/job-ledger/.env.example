# Example environment variables for the Job Ledger Vite client.
# Copy this file to `.env` and tweak values for your environment.

# URL of the Express API server (do not include a trailing slash).
# For Netlify deployments, set this to the hosted backend:
# https://jobledger.onrender.com
VITE_API_BASE_URL=https://jobledger.onrender.com

# During local development, Vite proxies requests starting with `/api`
# to this origin. Point it at your locally running Express server.
API_PROXY_TARGET=http://localhost:3001

# Controls the local-only sandbox data source. Use "off" to force all
# application CRUD operations to hit the real API.
VITE_APPLICATIONS_SANDBOX=sandbox

# Optional secret appended to `/jobs/refresh` when manually refreshing
# the cached job feed from the UI.
VITE_CRON_SECRET=
